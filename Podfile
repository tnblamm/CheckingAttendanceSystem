# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CheckingAttendanceSystem' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CheckingAttendanceSystem
target 'CheckingAttendanceSystem' do
pod 'Alamofire',
	:git => 'https://github.com/Alamofire/Alamofire.git',
	:branch => 'swift4'
end
  target 'CheckingAttendanceSystemTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CheckingAttendanceSystemUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
